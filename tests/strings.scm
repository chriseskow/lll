(print "simple string") # => simple string
(print "newline: [\n]") # => newline: [
                        # => ]
(print "tab: [\t]") # => tab: [	]
(print "backslash: [\\]") # => backslash: [\]
(print "double quote: [\"]") # => double quote: ["]
(print "invalid: [\a]") # => invalid: [\a]
